"""Command handlers for the LifeOS Work domain.

Handlers are grouped by the fact source they own.  The CLI composition root
(``lifeos_work.cli``) imports them and remains responsible for argparse
registration.
"""
